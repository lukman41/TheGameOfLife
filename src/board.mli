open Game

(** I think that this is where we should store the data for the board, were
    going to have define a bunch a spots and fill in the next for each spot in
    order to fill out a board. *)

(* i forgot we're doing a json so this is kinda redundant now *)
